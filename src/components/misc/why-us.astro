<section ui-section="why-us">
  <div class="head">
    <div class="headings">
      <div class="subtitle">
        <i class="hgi hgi-stroke hgi-body-part-muscle"></i> Why AlwanKit?
      </div>
      <h1 class="title">Everything a color system should be</h1>
    </div>
    <p class="description">
      One color is all you need. AlwanKit handles the palette, the contrast, the
      dark mode, and the export. You just build.
    </p>
  </div>
  <div class="core">
    <div class="card">
      <span class="index">/001/</span>
      <div class="info">
        <h3 class="label">Accessible by default</h3>
        <p class="detail">Every color meets WCAG standards automatically.</p>
      </div>
    </div>
    <div class="card">
      <span class="index">/002/</span>
      <div class="info">
        <h3 class="label">One color</h3>
        <p class="detail">
          From your base color, AlwanKit derives your entire palette.
        </p>
      </div>
    </div>
    <div class="card">
      <span class="index">/003/</span>
      <div class="info">
        <h3 class="label">Just CSS</h3>
        <p class="detail">A color system with no JavaScript dependency.</p>
      </div>
    </div>
    <div class="card">
      <span class="index">/004/</span>
      <div class="info">
        <h3 class="label">Take it anywhere</h3>
        <p class="detail">
          Export in CSS, SCSS, Tailwind, or JSON. Or fork the source and make it
          yours.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  [ui-section="why-us"] {
    display: grid;
    gap: var(--spacing-32);

    .head {
      display: flex;
      gap: var(--spacing-12);
      align-items: flex-end;
      justify-content: space-between;

      .headings {
        display: grid;
        gap: var(--spacing-8);

        .subtitle {
          font: var(--font-body-sm);
          border-radius: var(--radius-rounded);
          padding: var(--spacing-2) var(--spacing-8);
          background-color: var(--color-surface-container-lowest);
          box-shadow: var(--shadow-lg);
          display: flex;
          align-items: center;
          gap: var(--spacing-4);
          width: fit-content;

          & > i {
            color: var(--color-primary);
          }
        }

        .title {
          font: var(--font-headline-lg);
        }
      }

      .description {
        width: 30%;
        text-wrap: auto;
        text-align: justify;
        color: var(--color-on-surface-variant);
      }
    }

    .core {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      box-shadow: var(--shadow-lg);
      border-radius: var(--radius-lg);
      background-color: var(--color-surface-container-lowest);

      .card {
        padding: var(--spacing-24);
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        gap: var(--spacing-24);

        .index {
          color: var(--color-primary);
        }

        .info {
          display: grid;
          gap: var(--spacing-4);

          .label {
            font: var(--font-title-md);
          }

          .detail {
            color: var(--color-on-surface-variant);
          }
        }
      }
    }

    @media (min-width: 1024px) {
      .core {
        .card {
          &:not(:first-child) {
            border-inline-start: 1px solid var(--color-surface-variant);
          }
        }
      }
    }

    @media (max-width: 1024px) {
      .head {
        align-items: flex-start;
        flex-direction: column;

        .description {
          width: 100%;
          text-align: start;
          text-wrap: balance;
        }
      }

      .core {
        grid-template-columns: repeat(2, 1fr);

        .card {
          &:first-child,
          &:nth-child(2) {
            border-block-end: 1px solid var(--color-surface-variant);
          }

          &:nth-child(even) {
            border-inline-start: 1px solid var(--color-surface-variant);
          }
        }
      }
    }

    @media (max-width: 769px) {
      .head {
        .headings {
          .title {
            font: var(--font-title-lg);
          }
        }
      }

      .core {
        grid-template-columns: 1fr;

        .card {
          &:nth-child(3) {
            border-block-end: 1px solid var(--color-surface-variant);
          }

          &:nth-child(even) {
            border-inline-start: none;
          }
        }
      }
    }
  }
</style>
